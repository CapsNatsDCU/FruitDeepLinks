[pytest]
# Only the repository's own test suite belongs to this project.  This prevents
# an adjacent checkout (for example FruitDeepLinks/) from being collected and
# shadowing root-level test modules with identical names.
testpaths = tests
addopts = --import-mode=importlib
